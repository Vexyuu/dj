<form method="post" action="{{ path('app_dj_entity_delete', {'id': dj_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dj_entity.id) }}">
    <button class="btn">Supprimer</button>
</form>
